# 經驗資料庫（Lessons Learned）

> **唯一寫入者**：[lesson-keeper subagent](../.claude/agents/lesson-keeper.md)（PASS 終局時自動觸發；handoff 路徑由使用者裁決後手動觸發）
> **讀取者**：planner / executor 開工第一步必 grep；human 任意時點查閱
> **格式規範**：[plans/多代理協作工作流計劃.md §3.4](../plans/多代理協作工作流計劃.md)

---

## 三大寫入原則（每次新增條目前必過）

1. **原則性 > 數值性**：寫「為什麼錯」而非「這次改了哪個值」
2. **模式級 > 頁面級**：寫「哪種布局 / 架構模式容易踩」而非「某頁面要怎麼改」
3. **可遷移 > 可複製**：下個專案題材完全不同時，這條經驗仍要有用

自我檢查清單（lesson-keeper 寫入前必跑）：

- [ ] 這條原則替換成另一個專案、另一種題材後，文字本身仍成立嗎？
- [ ] 條目中是否出現具體數值（hex 色碼、px 數字、檔案路徑、變數名）？若是，能否抽象掉？
- [ ] 條目是否提到「這頁」「這個元件」「page14」這類專案級指涉？若是，能否改寫成「這類布局」「這類元件」？
- [ ] 「為什麼」段是否解釋了**根因**（為什麼這樣才對），而非只重述「就是這樣寫」？
- [ ] 本檔中是否已有同原則條目？若有，改去那條下面加 `相關案例:`，不要新開條目

---

## 索引（TOC）

> lesson-keeper 每次寫入時順手更新；類別 tag 強制單字（不可組合）以確保 grep 命中。

| 類別 | 條目 |
|---|---|
| architecture | _（尚無）_ |
| ui-layout | _（尚無）_ |
| testing | _（尚無）_ |
| api | _（尚無）_ |
| data-flow | _（尚無）_ |
| naming | _（尚無）_ |
| dependency | _（尚無）_ |
| security | _（尚無）_ |
| i18n | _（尚無）_ |

---

## 條目（按時間倒序追加）

> 第一輪安裝時為空。第一次跑完工作流且 PASS 後，lesson-keeper 會在此追加可遷移的經驗條目。

<!--
條目模板（lesson-keeper 用）：

## YYYY-MM-DD — <短標題（≤ 30 字、含關鍵詞便於 grep）>

- **類別**: ui-layout | architecture | testing | api | data-flow | naming | dependency | security | i18n
- **觸發來源**: plans/<功能名>計劃.md（第 N 輪 Codex 駁回） / wiki/runs/<時間戳>-<功能>.md
- **原則**: <一句、可遷移的規則>
- **為什麼**: <1–3 句根因與背後 trade-off>
- **適用情境**: <什麼時候會踩到？什麼時候反而不適用？>

❌ 反例（模式級描述，不寫專案特定值）：
> ...

✅ 正例：
> ...

（選填）相關案例:
- plans/<另一個功能名>計劃.md — 同原則的另一次踩坑
-->
